# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely need to edit after that.
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'jekyll serve'. If you change this file, please restart the server process.

# Site settings
title: Working at KBIA
email: digital@kbia.org
description: > # this means to ignore newlines until "baseurl:"
  Training for KBIA students, employees and community members.
baseurl: "/workingatkbia" # the subpath of your site, e.g. /blog
url: "http://apps.kbia.org" # the base hostname & protocol for your site

# Build settings
markdown: kramdown
gems: ['jekyll-less']

defaults:
  -
    scope:
      type: "posts"
    values:
      permalink: /:year/:slug/